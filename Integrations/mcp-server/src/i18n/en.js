// @ts-check
/** English dictionary: tool and field descriptions (read by the agent's model), result texts, errors, resources, prompts. */
export default {
  server: {
    instructions: 'This server gives an agent access to {productName} data per YouTrack project: the working sprint and its issues by role, history, capacity, calendar and absences, releases, reminders. Start with planner_get_project_overview — it reports the planner version, active roles and the state of the working slot. Units: effort and resources are minutes (8 h = 480), sprint and release dates are epoch-ms, absences are YYYY-MM-DD. Role, status and type codes are Latin only (resource planner://reference/enums). Passing baseRev is optional: the server reads it itself and retries a point operation once on a conflict; a full draft upload is never retried and refuses to overwrite an occupied slot without overwrite. Permissions are those of the token user in YouTrack and in the planner role groups. Compatibility: planner ≥ {contractMin}. {readOnly}',
    readOnlyNote: 'The server runs in read-only mode — there are no write tools.'
  },
  config: {
    header: 'Server not started — fix the environment variables:',
    baseUrl: 'YT_BASE_URL — YouTrack address of the form http(s)://host[:port] without a path or trailing "/"',
    appId: 'PLANNER_APP_ID — app name: lowercase Latin letters, digits and hyphen',
    transport: 'MCP_TRANSPORT — allowed values are http (shared server) and stdio (local run for debugging)',
    port: 'MCP_PORT — integer 0..65535 (0 — a free port, tests only)',
    timeout: 'YT_TIMEOUT_MS — integer milliseconds, 1000..600000',
    tokenInHttp: 'YT_TOKEN is set in http mode: a shared server holds no token — every client sends its own in the Authorization header; remove the variable',
    tokenMissingStdio: 'YT_TOKEN is required in stdio mode'
  },
  http: { noToken: 'Header Authorization: Bearer <permanent YouTrack token> is required' },
  fields: {
    projectKey: 'YouTrack project key (e.g. DEMO)',
    roleKey: 'Planning role key: analysis, testing, devPlatform, devBack, devFront, devIos, devAndroid, devFs, devDb',
    issueId: 'Readable YouTrack issue id, e.g. DEMO-101',
    baseRev: 'Data revision for optimistic locking. Omit — the server reads the current one and retries the operation once on a conflict; pass it — on a conflict you get rev_conflict with the current revision',
    resources: 'Available role resources in minutes: { "analysis": 4800, "devBack": 9600 }',
    title: 'Issue title; omit — the server fills it from YouTrack on write',
    inclusionStatus: 'Inclusion status: INC_PENDING (undecided), INC_PLANNED (planned), INC_UNPLANNED (unplanned), INC_EXCLUDED (excluded — excludeReason required)',
    estimate: 'Role estimate for the issue, minutes',
    fact: 'Role actual for the issue, minutes',
    alloc: 'Role allocation (reserved time) for the issue, minutes',
    excludeReason: 'Exclusion reason; required when inclusionStatus = INC_EXCLUDED',
    assignee: 'Assignee login (null — unassign); prefer planner_assign_person for assigning people',
    sprintName: 'Sprint name',
    dateStart: 'Start, epoch-ms (UTC midnight of the calendar date)',
    dateEnd: 'End, epoch-ms',
    sprintGoal: 'Sprint goal',
    roles: 'Roles taking part in the sprint; omit — all active project roles',
    sprintFieldVal: 'Value of the YouTrack "Sprint" field the sprint is bound to',
    versionFieldVal: 'Value of the version/release field, if the project uses one',
    sprintId: 'Stable sprint identifier (deduplication key in history), e.g. sprint-2026-11',
    absenceFrom: 'First day of absence, YYYY-MM-DD',
    absenceTo: 'Last day of absence, YYYY-MM-DD (single day — equals from)',
    absenceType: 'Type: vacation, sick, out_of_membership, regional_holiday, training, teamleading, other',
    hoursDelta: 'Partial absence — hours per day (0.5..24); omit — full day',
    grade: 'Participant grade (string from project settings)',
    rate: 'Rate 0..1',
    participation: 'Participation share 0..1',
    personAlloc: 'Participant allocation across roles, shares 0..1: { "analysis": 0.5, "testing": 0.5 }',
    releaseId: 'Release identifier (string up to 64 characters)',
    releaseKind: 'Kind: release or hotfix',
    releaseSource: 'Source: internal or vendor',
    releaseStatus: 'Status: planned → prep → work → released; cancelled — cancellation (release manager only)',
    plannedDate: 'Planned date, epoch-ms',
    freezeDate: 'Scope freeze date, epoch-ms',
    roleReps: 'Role representatives: { "analysis": "ivanov" }',
    includeExcluded: 'Show excluded issues (default yes)',
    includeSettings: 'Attach full project settings (a large object with rates and groups; default no)',
    limitPerRole: 'Maximum issues per role in the response (default 200); the response carries total and hasMore',
    sprintIdFilter: 'Filter by sprint identifier (role records sprintId_roleKey match too)',
    sprintStatus: 'Filter by sprint status: PLANNING, CONFIRMED, ALLOCATED, FINISHED',
    limit: 'Maximum records in the response',
    includeItems: 'Attach the records\' issues — only together with sprintId',
    sprintIdDefault: 'Sprint identifier; omit — the sprint of the working slot',
    includeArchive: 'Attach the archive',
    year: 'Only one calendar year, e.g. 2026',
    loginFilter: 'Only one employee (login)',
    periodFrom: 'Only absences overlapping the period: start YYYY-MM-DD',
    periodTo: 'Only absences overlapping the period: end YYYY-MM-DD',
    releaseStatusFilter: 'Only releases in this status',
    includeJournal: 'Attach the project reminders journal',
    sprint: 'Sprint header: sprintId, name, dateStart, dateEnd; optional sprintGoal, roles, sprintFieldVal, versionFieldVal, resources. Status is always PLANNING',
    roleItems: 'Issues by role: { "analysis": [ { "issueId": "DEMO-101", "inclusionStatus": "INC_PLANNED", "estimate": 480 } ] }. Only roles from the project activeRoles',
    overwrite: 'true — write over an occupied slot (someone else\'s draft or a sprint in progress). Default false — refusal slot_occupied',
    item: 'Issue: issueId is required; the rest — what to change (merged with the stored record)',
    sprintPatch: 'Header keys to change (name, dateStart, dateEnd, sprintGoal, sprintFieldVal, versionFieldVal, resources). A sprint cannot be created this way',
    assignLogin: 'Assignee login; null — unassign the person',
    assignDateStart: 'Start of work on the issue, epoch-ms; null — clear the date',
    assignDateEnd: 'End of work on the issue, epoch-ms; null — clear the date',
    login: 'Employee login in YouTrack',
    entry: 'Absence: from, to, type; optional hoursDelta. A record with the same from and to is replaced entirely',
    person: 'Capacity participant parameters: grade, rate, participation, alloc — only what needs changing',
    release: 'Release: id is required; the rest — what to write (merged with the stored record; null removes a value)',
    snapshot: 'Closing snapshot — only when moving to released',
    issuesAdd: 'Issue ids to add to the release',
    issuesRemove: 'Issue ids to remove from the release',
    projectKeys: 'YouTrack project keys (shortName), up to 5000, e.g. ["DEMO", "SP"]'
  },
  tools: {
    planner_get_project_overview: { title: 'Project overview in the planner', description: 'First call for a project: planner version and compatibility with this server, whether the planner is configured, active roles, the working sprint header with issue counts per role, whether sprint creation is locked, release count and registry revision. Read-only.' },
    planner_get_sprint: { title: 'Working sprint and issues', description: 'The project working slot: sprint header (status, dates, goal, role resources in minutes, revision rev) and issues by role in compact form (issueId, title, state, inclusion status, assignee, role estimate/actual/allocation in minutes). Filter by roleKey, per-role limit, excluded flag. Project settings — only with includeSettings. Read-only.' },
    planner_get_history: { title: 'Sprint history', description: 'Sprint snapshots by role from the project history: status, dates, who confirmed and finished and when, over-limit flag, issue count. Filters: sprintId, roleKey, status; record limit; record issues — includeItems together with sprintId. The response carries the history rev. Read-only.' },
    planner_get_capacity: { title: 'Sprint capacity', description: 'Sprint capacity record: participants with grade, rate, participation share, allocation across roles and base hours; approval status. Without sprintId the working slot sprint is used; the record may be missing (capacity: null). Archive — by flag. Read-only.' },
    planner_get_calendar: { title: 'Production calendar', description: 'The project production calendar by year (days with type and hours). The year parameter narrows to one year. Read-only.' },
    planner_get_absences: { title: 'Employee absences', description: 'Absence registry by login: from, to, type, hoursDelta. Filters: login and period (from/to — absences overlapping the period). The response carries the registry rev. Read-only.' },
    planner_get_releases: { title: 'Project releases', description: 'Release registry: id, name, kind, source, status, dates, freeze, issue list, role representatives; the caller\'s write permissions (perms) and registry rev. Filter by status; archive — by flag. Read-only.' },
    planner_get_reminders: { title: 'Planner reminders', description: 'Active reminders addressed to the token user: module (sprints, capacity, releases), kind, entity, in how many days or how many days ago. The project reminders journal — by flag. Read-only.' },
    planner_upload_draft: { title: 'Upload a sprint draft', description: 'Write a sprint draft into the project working slot entirely: header (status forced to PLANNING) and issues by role. The server checks that the roles are active in the project and refuses (slot_occupied) when the slot holds someone else\'s draft or a sprint in progress — then overwrite:true is needed. An issue needs only issueId and an estimate: title, state and priority are filled by the server from YouTrack (up to 200 issues per request, counter enriched). Role: editor. A full write is not retried on a revision conflict.' },
    planner_upsert_item: { title: 'Add or change a role issue', description: 'Point operation: add an issue to a role or change its keys (merge: what is sent is updated, the rest is kept). Exclude with a reason — inclusionStatus INC_EXCLUDED and excludeReason. Role: editor. Reads the revision itself and retries once on a conflict.' },
    planner_remove_item: { title: 'Remove an issue from a role', description: 'Point operation: remove an issue from the role issue list. Missing issue — item_not_found. Role: editor.' },
    planner_patch_sprint: { title: 'Change the sprint header', description: 'Point operation: change part of the working sprint header: name, dates, goal, field values, role resources (minutes). A sprint cannot be created this way — use planner_upload_draft. Role: editor.' },
    planner_assign_person: { title: 'Assign a person', description: 'Assign a person (login) to a role issue and optionally the work dates; login null — unassign. Works once the role was sent for confirmation or the sprint is running — otherwise role_record_not_found. Role: assigner. The response carries the slot rev and historyRev.' },
    planner_upsert_absence: { title: 'Add or replace an absence', description: 'Point operation: add an employee absence (from, to, type, hoursDelta); a record with the same dates is replaced entirely. Role: settings manager or planning manager.' },
    planner_remove_absence: { title: 'Remove an absence', description: 'Remove an employee absence by login and the from/to date pair. No such record — absence_not_found. Role: settings manager or planning manager.' },
    planner_upsert_capacity_person: { title: 'Add or change a capacity participant', description: 'Point operation: add a participant to the sprint capacity record or change their grade, rate, participation share, allocation across roles. Without sprintId — the working slot sprint; the sprint must have dates. Capacity has no revision. Role: settings manager or planning manager.' },
    planner_upsert_release: { title: 'Create or change a release', description: 'Create a release or change an existing one by id (key merge; null removes a value). Role: release manager; a release engineer may only move an existing release to the next status in the chain.' },
    planner_set_release_status: { title: 'Change release status', description: 'Move a release to a status: chain planned → prep → work → released, cancelled — cancellation. A release engineer — only to the next chain step; snapshot — only together with released. Role: release manager or release engineer.' },
    planner_update_release_issues: { title: 'Change release issues', description: 'Add (add) and/or remove (remove) release issues in one call; executed as two point operations chained by revision. Role: release manager.' },
    planner_filter_projects: { title: 'Projects with the planner attached', description: 'From the given project keys, return those where the planner is attached and the user may read: key, name, disabled (the planner is turned off — such a project is visible only to those who can turn it back on). The tool does not list the instance\'s projects — get them with the standard YouTrack tools and pass the keys here (up to 5000). The answer is always current, nothing is cached. Read-only.' },
    planner_get_my_roles: { title: 'My roles in the project', description: 'The token user\'s roles in the project by planner groups (true — the user has the role): editor, assigner, validator, historyManager, settingsManager, planningManager, releaseManager, releaseEngineer, sprintLockManager; configured — the planner is set up in the project; instanceAdmin — YouTrack administrator (every role except history management). Call it before writing to avoid a permission refusal. A role is group membership: some operations accept several roles, each tool\'s description names its role. Read-only.' },
    planner_remove_release: { title: 'Delete a release', description: 'Delete one release from the active registry by id; other releases, the archive and YouTrack issues are not changed. Irreversible — it can only be recreated with planner_upsert_release. No such release — release_not_found. Role: release manager.' }
  },
  result: {
    overview: 'Project {projectKey}: planner {version}; slot — {sprint}; releases {releases}.',
    slotEmpty: 'the working slot is empty (revision 0)',
    contractOld: 'Warning: planner {version} is older than the minimum version {min} — some operations may be unavailable.',
    sprint: 'Sprint "{name}" ({status}), revision {rev}; issues by role: {counts}.',
    history: 'History records: {count} of {total}.',
    itemsNeedSprintId: 'Issues are attached only together with sprintId.',
    capacity: 'Capacity of sprint {sprintId}: participants {persons}, status {status}.',
    capacityNone: 'No capacity record for sprint {sprintId}.',
    calendar: 'Calendar: years {years}.',
    absences: 'Absences: employees {people}, records {entries}.',
    releases: 'Releases: {count}, registry revision {rev}.',
    reminders: 'Active reminders: {count}.',
    remindersOff: 'The reminders master switch is off in this project.',
    written: '{action}: written, revision {rev}.',
    retried: 'There was a revision race — the operation was retried with the current revision.',
    uploaded: 'Draft {sprintId} written, revision {rev}; warnings: {warnings}.',
    releaseIssues: 'Release {id}: added {added}, removed {removed}; revision {rev}.',
    filterProjects: 'With the planner attached: {count} of {total} given projects.',
    myRoles: 'Project {projectKey}: roles — {roles}.',
    truncated: 'Text truncated to {limit} characters — narrow the filters (roleKey, sprintId, limit); the structured response is complete.'
  },
  errors: {
    refusal: 'Planner refusal {reason}: {meaning} What to do: {action} cid={cid}.',
    noHint: 'no description of this code in the registry.',
    revConflictTail: 'Current revision is {rev}: re-read the data and retry with it.',
    errorsTail: 'Details: {errors}',
    platform: {
      '401': 'YouTrack rejected the token (HTTP 401): the token is missing, invalid or expired.',
      '404': 'YouTrack answered 404 "HTTP handler not found": the planner is not attached to this project, or the app name (PLANNER_APP_ID) or address is wrong.',
      other: 'The YouTrack platform answered HTTP {status} — this is not a planner refusal; check the address and instance availability.'
    },
    network: 'YouTrack is unreachable at {baseUrl}: network or DNS.',
    timeout: 'YouTrack did not answer in time ({baseUrl}) — retry later.',
    unexpected: 'Internal server error: {message}',
    guard: {
      slot_occupied: 'The working slot is occupied: sprint {sprintId} in status {status} (revision {rev}). The upload was refused so as not to overwrite someone else\'s work. If writing over it is intended, repeat with overwrite:true; to edit the current sprint use point operations.',
      role_not_active: 'Role {roleKey} is not planned in this project; active roles: {activeRoles}. Fill only those.',
      project_not_allowed: 'Project {projectKey} is not in the list allowed for this server.',
      read_only: 'The server is in read-only mode.',
      sprint_not_found: 'There is no sprint in the working slot — pass sprintId explicitly or create a sprint.',
      nothing_to_do: 'The request contains no changes: pass at least one key.'
    }
  },
  resources: {
    openapi: { title: 'External REST contract (OpenAPI)', description: 'openapi-sprint.yaml of the edition this server was built with: all operations, bodies and responses' },
    errorCodes: { title: 'Refusal codes', description: 'Published reason codes with class, meaning and recommended action' },
    roles: { title: 'Planning roles', description: 'Nine role keys with labels and resource keys' },
    enums: { title: 'Enumerations', description: 'Sprint and inclusion statuses, absence types, release statuses and kinds, phase keys, units of measure' },
    quickstart: { title: 'Agent quickstart', description: 'How to work with the planner through this server: call order, units, revisions, roles' }
  },
  prompts: {
    planner_upload_draft_guide: { title: 'Upload a sprint draft', description: 'Step-by-step scenario for uploading a draft into the project working slot',
      text: 'Upload a sprint draft into project {projectKey}. Order: 1) planner_get_project_overview — make sure the planner is configured (configured), learn activeRoles and the slot state; if the slot holds a sprint in progress — stop and ask whether writing over it is allowed. 2) Build the header (sprintId, name, dateStart/dateEnd in epoch-ms, sprintGoal, resources in minutes) and the issues only for active roles: an issue needs issueId, inclusionStatus and estimate in minutes — the server fills the title itself. 3) planner_upload_draft. 4) Check the response: rev, enriched, warnings; on a refusal read reason and the hint, fix the body and retry. 5) Report briefly: what was written, how many issues per role, which warnings.' },
    planner_point_changes_guide: { title: 'Change the issue list point by point', description: 'Scenario for point edits: add an issue, exclude with a reason, assign a person',
      text: 'Make point changes to the working sprint of project {projectKey}. Order: 1) planner_get_sprint with the needed roleKey — look at the current issues and revision. 2) One point operation per change: planner_upsert_item (add/change, exclude with excludeReason), planner_remove_item, planner_patch_sprint (header), planner_assign_person (assignee and dates). Do not pass baseRev — the server reads the revision itself and retries on a race. 3) Do not resend the whole issue list and do not use planner_upload_draft to edit a running sprint. 4) Finally list the performed operations with their revisions.' },
    planner_release_flow_guide: { title: 'Manage releases externally', description: 'Release scenario: create, fill with issues, advance the status',
      text: 'Run a release in project {projectKey}. Order: 1) planner_get_releases — the registry, the caller\'s perms and revision. 2) planner_upsert_release — create the release (id, name, kind, source, status planned, plannedDate). 3) planner_update_release_issues — add issues (add) and remove if needed (remove). 4) planner_set_release_status — along the chain planned → prep → work → released; snapshot only together with released; cancelled — only on explicit request. A release engineer may only take the next chain step. 5) Report: id, status, issues, revision.' }
  },
  guide: {
    quickstart: '# Agent quickstart: {productName} via MCP\n\n1. Start with `planner_get_project_overview` — planner version (must be ≥ {contractMin}), `configured`, `activeRoles`, slot state.\n2. Units: effort and resources — minutes (8 h = 480); sprint, release and assignment dates — epoch-ms; absences — YYYY-MM-DD.\n3. Codes are Latin only: roles (`analysis`, `testing`, `devPlatform`, `devBack`, `devFront`, `devIos`, `devAndroid`, `devFs`, `devDb`), sprint statuses (`PLANNING`, `CONFIRMED`, `ALLOCATED`, `FINISHED`), inclusion (`INC_PENDING`, `INC_PLANNED`, `INC_UNPLANNED`, `INC_EXCLUDED`), releases (`planned` → `prep` → `work` → `released`, `cancelled`).\n4. Revisions: `baseRev` may be omitted — the server reads it and retries a point operation once on a race. A full draft upload is never retried and does not overwrite an occupied slot without `overwrite`.\n5. A running sprint is edited with point operations (`planner_upsert_item`, `planner_remove_item`, `planner_patch_sprint`, `planner_assign_person`), not with a full upload.\n6. A refusal comes with a `reason` code, a hint and a `cid` — by `cid` an administrator finds the record in the planner server log.\n7. Permissions: reading — any project member; writing — by planner role groups (editor, assigner, settings manager, planning manager, release manager, release engineer). The server adds no permissions. Your roles in a project — `planner_get_my_roles`; which projects have the planner attached — `planner_filter_projects` (project keys come from the standard YouTrack tools).'
  }
};
