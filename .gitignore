node_modules/
playwright-report/
test-results/
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*.log
# Playwright auth state — session cookies, never commit
playwright/.auth/user.json
playwright/.auth/*.json

# Claude Design internal mirror — не публикуем в public GitHub
design/
marketplace-screenshots/

# Per-release smoke screenshots — local only, не коммитятся
tests/screenshots/

# Internal session prompts — не публикуем в public GitHub
Prompts/

# MCP server (#113): local evals against stand data and the container env file
Integrations/mcp-server/evals/
Integrations/mcp-server/.env
