/* v5.12.0 — OQ44 placeholder: APP_VERSION will migrate here after full IIFE→ESM conversion.
   Currently APP_VERSION lives in core.js (esbuild IIFE closure can't inject
   a named export as a free variable into a nested IIFE).
   When monolith is fully converted to ESM: import APP_VERSION from here everywhere.
   For now: keep this file for documentation and future use. */
// export const APP_VERSION = '5.12.0'; // activate after IIFE removal
