{
  "$schema": "https://json.schemastore.org/youtrack-app.json",
  "name": "smart-sprint-planner",
  "title": "Smart Sprint Planner",
  "description": "Multi-role sprint planning plugin for YouTrack 2024.3+. Supports 9 functional roles with separate composition tables, resources, and history snapshots. Plugin settings live in a dedicated widget with server-side authorization.",
  "url": "https://github.com/Letsrollamigo/smart-sprint-planner",
  "vendor": {
    "name": "Letsrollamigo",
    "url": "https://github.com/Letsrollamigo",
    "email": "Oberon999@yandex.kz"
  },
  "version": "2.16.3",
  "icon": "app-logo.svg",
  "iconDark": "app-logo-dark.svg",
  "minYouTrackVersion": "2024.3.0",
  "settings": "settings.json",
  "changeNotes": "<b>🐞 v2.16.3 — Editor buttons stuck in a disabled look</b><br><br>In the role cards, the «Pick tasks» button (and other editor buttons) could appear disabled even though clicking still worked — this happened when several role cards were expanded right after the planner opened. Editor rights are now applied to all role cards at once, so the buttons render in their correct state.<br><br><b>🎨 v2.16.2 — Backlog role-grid layout + menu icon</b><br><br>The «Pipeline zones» role grid in backlog settings now lays roles out in a tidy, dense grid when many roles are active. Previously, with all 9 roles enabled the checkboxes stretched to fill the cell and pushed their labels into a one-character-per-line mess — a shared field-width rule was wrongly stretching the checkboxes (the root cause, now excluded for checkboxes). Plus: the YouTrack main-menu item now shows the planner's brand icon instead of the default placeholder glyph.<br><br><b>🔧 v2.16.1 — Capacity tab visibility hotfix</b><br><br>A follow-up to 2.16.0: the «Capacity» tab (calendar, absences and personal-capacity calculation) now appears in the navigation again when the «Full» planning model is selected. A missing settings accessor meant the navigation node was never built, making the whole tab unreachable. Affects only the «Full» model; «Light»/«Simple» are unchanged.<br><br><b>✨ v2.16.0 — Full capacity mode + sprint-state fixes</b><br><br>🧮 <b>Full capacity model</b> — The planning model now offers a «Full» mode: the planner consumes the approved business capacity per role and per assignee — the role remainder, the «Available resources» field and the accordion header all read the approved capacity consistently.<br><br>🐞 <b>Sprint-state cluster fix</b> — Adding a fresh second role no longer triggers a false «Select a sprint» prompt; deleting the last role record of the active sprint now removes the sprint completely (active state, server snapshot and per-user draft), so it no longer lingers in the picker or reappears after a hard reload.<br><br>🧹 <b>Under the hood</b> — Removed duplicate i18n keys across all 15 locales (clean build).<br><br><b>🎨 v2.15.3 — Backlog settings polish</b><br><br>Fixed the «Pipeline zones» layout (role labels no longer overlap the row controls), «Pause tags» are now chosen from a searchable list of ALL instance tags (previously free-text, and the API returned at most ~42), and the settings option font size is now uniform.<br><br><b>🐞 v2.15.2 — Legacy settings fix</b><br><br>Projects first configured in an early plugin version could no longer save their settings — every save (including the silent auto-save on open) failed with «invalid_settings_structure». The deprecated «dev1c» functional role and its orphaned settings keys are now remapped to «devPlatform» on read (values preserved), so saving works again.<br><br><b>🐞 v2.15.1 — Settings state-picker fix</b><br><br>The «Stand-up assist» (done states), «State rollup» and «Backlog» settings pickers now load the list of available states as soon as you pick or change the «State» field — previously the list was fetched once when the form opened and the picker could stay empty.<br><br><b>✨ v2.15.0 — Backlog module (#21)</b><br><br>📋 <b>Backlog workspace</b> — A pre-planning phase before sprint planning: pull tracker tasks into a pool grouped by pipeline zones (state → role) or as an <b>Epic ▸ Story ▸ Task</b> tree, switch views, and filter with YouTrack query-assist.<br><br>🎯 <b>Lay into sprint</b> — Send a pooled task into the sprint's role composition (auto-suggested roles by zone mapping + manual edit); per-role load bar (demand vs role resource); «Needs estimate», «Carryover»/«Continuation» (from transition history) and «Paused» labels.<br><br>🚦 <b>Fail-loud</b> — Tasks in unmapped states aren't lost: an «Other» bucket plus a warning that lists the unmapped states to configure.<br><br><b>✨ What's new since v2.5.6</b><br><br>🔐 <b>Settings access tiers</b> — Project settings are now split into «Planning» and «Administration» groups. Workflow-rule configuration (DTA, cascade aggregation, container-work ban, State Rollup) and access-rights groups are editable only by the settings manager; a new «Planning settings manager» group lets you delegate planning settings (roles, fields, norms, modes) without granting workflow access.<br><br>🎭 <b>Per-role sprint status</b> — Sprint status is now tracked independently for each role: lowering one role's status (e.g. via the overlimit downgrade) no longer affects the others, per-role status badges update immediately, and the composition lock is per-role too.<br><br>🐞 <b>History &amp; planning fixes</b> — The «Allocation» and «Assignee» columns are back in sprint-history records; sprint intro fields (name / dates / goal) now populate on a cold load; the «Reset draft» button reliably clears the server draft; the working-copy banner and indicator hide when no working copy is active.<br><br>♿ <b>Accessibility</b> — Sprint-history record headers are now keyboard-operable (Enter / Space) and announced as expandable buttons by screen readers; the pick-assignees toast no longer shows doubled punctuation. Button labels now match their spoken names — the «Pick assignees», «Calculate resource», «Refresh from task» and «Clear» buttons are announced by screen readers and recognized by voice input exactly as they read on screen (WCAG 2.5.3 «Label in Name»).<br><br>🛠 <b>Under the hood</b> — Widget source reorganized into architecture layers (domain / infra / pure / data / i18n) with automated fitness checks guarding the structure; the test suite moved to Node's built-in runner (node --test: unit + golden, no browser) with GitHub Actions CI and a CONTRIBUTING guide. The runtime bundle is byte-identical — a pure internal refactor. The personal-planning data model is now unified onto per-role history snapshots as the single source of truth, eliminating a latent keyed-vs-single form mismatch in persistence (no user-visible change).<br><br>🔍 <b>Compatibility</b> — YouTrack 2024.3+. Drop-in upgrade — no schema migration, no data loss. Existing project settings (roles, fields, manager group) are reused as-is.",
  "widgets": [
    {
      "key": "ssp-main",
      "name": "Smart Sprint Planner",
      "description": "Planner settings for the project: functional roles, permission groups (validation / editing / assignee assignment / history clearing), estimate and spent fields, planning modes. Sprint planning itself lives in the main-menu item «Smart Sprint Planner».",
      "extensionPoint": "PROJECT_SETTINGS",
      "indexPath": "widgets/main/index.html",
      "iconPath": "app-logo.svg"
    },
    {
      "key": "ssp-main-global",
      "name": "Smart Sprint Planner",
      "description": "Sprint planning from YouTrack's main menu: pick an accessible project where the planner is set up, plan sprint composition by role, allocate tasks, review history. Permissions and settings are checked against the selected project. Includes a «Backlog» module for triaging and laying pool tasks into roles/sprint.",
      "extensionPoint": "MAIN_MENU_ITEM",
      "indexPath": "widgets/main/index.html",
      "iconPath": "app-logo.svg"
    }
  ]
}
